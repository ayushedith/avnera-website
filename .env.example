# DATABASE_URL points your app to the DB (can be Prisma Accelerate or direct Postgres)
# Example (Accelerate):
# DATABASE_URL="prisma+postgres://<accelerate-url>?api_key=<key>"
# Example (Direct Postgres):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public"

# DIRECT_URL is required when using Accelerate for migrations (points to direct Postgres)
# DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public"
